{% extends 'base.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}

    <div class="container">
        <h1>Your Image is generated by openAI</h1>
        <h3>{{ image.title }}</h3>
        <img src="{{ image.url }}">
    </div>
    <div class="container">
        <a href="{{ path('app_image_index') }}">back to list</a>
    </div>
{% endblock %}
