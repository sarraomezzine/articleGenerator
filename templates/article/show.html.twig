{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <div class="container">
        <h1>Your Article is generated by openAI</h1>
        <h2>{{ article.title }}</h2>
        <p>{{ article.description }}</p>
        <a href="{{ path('app_article_index') }}">back to list</a>
    </div>
    


{% endblock %}
